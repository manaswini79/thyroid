services:
  - type: web
    name: thyrolead
    env: docker
    plan: free
    region: singapore
    repo: https://github.com/manaswini79/thyroid
    branch: main
    dockerfilePath: ./Dockerfile # Ignored since Docker Compose is used
    dockerContext: .
    autoDeploy: true
